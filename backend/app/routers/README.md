# Routers

Implement FastAPI routes based on `../api_spec.yaml`.

Suggested files:
- `transactions.py` — CRUD + soft delete + /quick endpoint
- `categories.py` — GET /categories
- `budgets.py` — POST /budgets, GET /budgets/current
- `summary.py` — GET /summary/daily, /monthly, /yearly