# step-1-db-schema

Generated by MetrixAgents on 2026-03-20 08:25 UTC

## Quick Start

### Prerequisites
- Docker & Docker Compose
- Node.js 20+ (for local dev)
- Python 3.12+ (for local dev)

### Run with Docker
```bash
cp backend/.env.example backend/.env
# Edit backend/.env with your ANTHROPIC_API_KEY
docker-compose up --build
```
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

### Run locally

**Backend:**
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

## Project Structure

| File | Generated by |
|------|-------------|
| `backend/app/models.py` | db_agent |
| `backend/api_spec.yaml` | api_designer |
| `frontend/components/Dashboard.tsx` | ui_designer |
| `frontend/lib/store.ts` | state_agent |

## Next Steps

1. Implement FastAPI routes in `backend/app/routers/` based on `api_spec.yaml`
2. Run Alembic migrations (see `backend/migrations/README`)
3. Configure auth (JWT) in `backend/app/main.py`
4. Connect frontend components to real API endpoints